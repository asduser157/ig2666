#include stdio.c
#include int main(void)
{
    string name = get_string("What is yoyur first name\n");
    int num = 0;
    while (name[n] != '\0';)
    {
        n = n + 1;
    }
    printf("%i", num);
}