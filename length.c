#include<stdio.h>
#include<cs50.h>
#include<string.h>
int main(void)
{
    string name = get_string("What is yoyur first name\n");
    int num = 0;
    while (name[num] != '\0');
    {
        n = n + 1;
    }
    printf("%i", num);
}